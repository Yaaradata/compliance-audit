/**
 * Home dashboards: same max width on all PCs/laptops (avoids stretched layouts on ultrawide/4K).
 * Centered so padding aligns with the app shell.
 */
export const DASHBOARD_MAX_WIDTH_CLASS = "w-full max-w-[1920px] mx-auto";
