/**
 * Azure evidence area is not available yet — keep route for future deep links without showing UI.
 */
export default function AzureEvidencePlaceholderPage() {
  return <div className="min-h-[min(60vh,480px)] w-full" aria-hidden />;
}
