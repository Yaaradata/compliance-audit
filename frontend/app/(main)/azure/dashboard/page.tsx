/**
 * Azure evidence dashboard is not available yet — keep route without showing details.
 */
export default function AzureDashboardPlaceholderPage() {
  return <div className="min-h-[min(60vh,480px)] w-full" aria-hidden />;
}
