'use client';

/**
 * ORI prototype — kept in sync with routed app by re-exporting the same root component.
 * (Historical monolithic bundle removed; mock + UI live under `components/` + `lib/`.)
 */
import IndianBankingAuditApp from '../../components/IndianBankingAudit/IndianBankingAuditApp';

export default IndianBankingAuditApp;
