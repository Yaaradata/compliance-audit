/**
 * UK Process Audit version routing — shared by the select-region card.
 */
export {
  UKPA_BASE_PATHS as UK_PROCESS_AUDIT_PATHS,
  UKPA_VERSION_ORDER as UK_PROCESS_AUDIT_VERSION_ORDER,
  LATEST_UKPA_VERSION as LATEST_UK_PROCESS_AUDIT_VERSION,
  UKPA_VERSION_SELECT_LABELS as UK_PROCESS_AUDIT_VERSION_SELECT_LABELS,
  type UkpaVersion as UkProcessAuditVersion,
} from "@/components/UK_Process_Audit/ukpa/ukpaVersion";
