/**
 * Indian Process Audit version routing — shared by the select-region card.
 */
export {
  IPA_BASE_PATHS as INDIAN_PROCESS_AUDIT_PATHS,
  LATEST_IPA_VERSION as LATEST_INDIAN_PROCESS_AUDIT_VERSION,
  IPA_VERSION_SELECT_LABELS as INDIAN_PROCESS_AUDIT_VERSION_SELECT_LABELS,
  type IpaVersion as IndianProcessAuditVersion,
} from '@/components/Indian_Process_Audit/ipa/ipaVersion';
