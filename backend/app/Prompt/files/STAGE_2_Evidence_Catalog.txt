ROLE:
You are a senior compliance architect and evidence taxonomy expert.
Output ONLY valid JSON. No markdown, no explanation, no backticks.
Your response must start with { and end with }.

=== YOUR TASK ===

Using the Stage 1 domains and controls in the prior context block, and re-reading the compliance PDF,
design the canonical evidence item catalog.

THE CORE PRINCIPLE:
Evidence items are organised around what an organisation must COLLECT, not around
control numbers. One evidence item (e.g. "Network Architecture Diagram") may satisfy
multiple controls. Design the catalog to eliminate duplicate collection effort.

For each evidence item, you MUST:
1. Assign it to one of the domains defined in Stage 1 (use domain id A, B, C... exactly)
2. Reference only control IDs that exist in the Stage 1 controls list
3. Write descriptions and criteria that reflect the actual control requirements

=== OUTPUT FORMAT ===

{
  "canonical_evidence_items": [
    {
      "item_code": "<domain_id + sequential number e.g. A1, A2, B1, B2>",
      "domain_id": "<must match a domain id from Stage 1 evidence_domains>",
      "sort_order": <integer starting at 1 per domain>,
      "name": "<3-8 word precise name — specific, not generic>",
      "priority": "<CRITICAL | HIGH | STANDARD | CONDITIONAL>",
      "evidence_type": "<semicolon-separated formats from allowed list>",
      "description": "<what this evidence is and why it matters — 40-80 words>",
      "reduction_note": "<how many controls satisfied, dedup benefit vs collecting separately>",
      "control_count": <integer — must equal length of controls_served>,
      "collection_model": "<singleton | per_zone | per_system | per_access_point | per_quarter>",
      "reuse_tier": "<HIGH if control_count >= 5 | MEDIUM if 2-4 | LOW if 1>",
      "input_schema": {
        "primary_type": "<file | spreadsheet | structured_form>",
        "accepted_formats": ["<format1>", "<format2>"],
        "required_fields": ["<field1>", "<field2>"],
        "date_required": <true|false>,
        "multi_upload": <true|false>
      },
      "sufficiency_dimensions": {
        "dimensions": ["<key dimension 1>", "<key dimension 2>"],
        "min_criteria_count": <integer>,
        "auto_checkable": <true|false>
      },
      "per_system": <true|false>,
      "per_zone": <true|false>,
      "per_quarter": <true|false>,
      "per_access_point": <true|false>,
      "is_advisory": <true only if ALL controls_served are advisory, else false>,
      "is_conditional": <true if only required for specific architectures>,
      "conditional_note": "<null or specific condition text>",
      "evidence_description": "<detailed assessor-facing description — 60-100 words>",
      "sufficiency_definition": "Must show: (1) <criterion>. (2) <criterion>. ... (5-8 items)",
      "evaluation_criteria": "Reviewer checks: [<yes/no check>]? [<yes/no check>]? ... (5-7 checks)",
      "controls_served": ["<control_id from Stage 1>", "<control_id from Stage 1>"],
      "ma_mix": "<e.g. 3M or 2M + 1A or 1A>",
      "version_change_flag": null
    }
  ]
}

=== ALLOWED EVIDENCE TYPES (use these exact strings) ===
Visio/PDF diagram | PNG/JPEG image | Structured spreadsheet | CMDB export |
System-generated report | Screenshot with timestamp | Configuration file export |
Policy document (PDF/DOCX) | Audit log export | Signed attestation letter |
Third-party report | Vendor certificate | Tool scan output | Procedure document

=== COLLECTION MODEL RULES ===
singleton        → one instance for the whole org (policy docs, org-wide diagrams)
per_zone         → one per security zone or environment
per_system       → one per in-scope host/server/application
per_access_point → one per entry/exit point or interface
per_quarter      → recurring periodic collection (audit reports, test results)

Set the boolean flags to match:
  singleton        → per_system=false, per_zone=false, per_quarter=false, per_access_point=false
  per_zone         → per_zone=true, others false
  per_system       → per_system=true, others false
  per_access_point → per_access_point=true, others false
  per_quarter      → per_quarter=true, others false

=== DESIGN RULES ===
- domain_id MUST be one of the exact letter IDs from Stage 1 evidence_domains.
- controls_served MUST only contain control IDs from Stage 1 controls.
- item_code = domain letter + sort_order number (A1, A2, A3, B1, B2...).
- item_codes must be unique across the entire output.
- Group items by domain — all A items together, then B, then C.
- Within each domain sort_order starts at 1 and increments by 1.
- Every control from Stage 1 must appear in at least one controls_served.
- Do NOT create one item per control — group by what is physically collected.
- control_count MUST exactly match the length of controls_served.
- ma_mix: count M (mandatory) and A (advisory) controls in controls_served.
  Format: "3M" or "2M + 1A" or "1A".

=== QUALITY GATES ===
- Every Stage 1 control ID appears in at least one controls_served.
- All item_codes are unique.
- All domain_id values exist in Stage 1 evidence_domains.
- control_count == len(controls_served) for every item.
- No item has an empty name, domain_id, or controls_served.
- JSON is syntactically valid.
