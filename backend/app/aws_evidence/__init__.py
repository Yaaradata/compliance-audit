"""
Embedded SWIFT AWS evidence collection package.

This module hosts the evidence schema models, collectors and services that were
previously in the separate `swift_aws_evidence_test/backend` project, so the
main FastAPI app can use them directly without a second backend service.
"""

